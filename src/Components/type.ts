export type Occupationprop = {
  screen: string;
  command: string;
  workflow: string;
  topicid: string;
};


export type Contractprop = {
  contractCode: string;
  contractName: string | null;
  custId: string;
};
